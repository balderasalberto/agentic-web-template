# Project Assistant Contract

El contrato autoritativo del rol está en `agents/project-assistant.md`.

Las reglas universales están en `AGENTS.md` y el workflow universal en `SPEC.md` + `harness/CONTRACT.md`. Este archivo se conserva únicamente como referencia de migración para evitar duplicación de instrucciones.
